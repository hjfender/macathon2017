<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Brand</a>
    </div>
    <div class="navbar-header navbar-right">
      <p class="navbar-text">Signed in as <%= @doctor.name %></p>
    </div>
  </div><!-- /.container-fluid -->
</nav>

<div style="margin:20px" class="jumbotron">
    <div class="container">
  <h2>Select an Appointment</h2>
  <table class="table">
    <thead>
      <tr>
        <th>Clinic</th>
        <th>Sliding Scale</th>
        <th>Doctor Name</th>
        <th>Doctor Gender</th>
        <th>Specialty</th>
        <th>Date</th>
      </tr>
    </thead>
    <tbody>
      <% @appointments.each do |appointment| %>
        <tr>
          <% if appointment.open? && (@doctors.find(appointment.doctor_id).specialty == "Pediatric" || @doctors.find(appointment.doctor_id).specialty == "Optometry") %>
            <td> <%= @organizations.find(@doctors.find(appointment.doctor_id).organization_id).name%></td>
            <td> <%= @organizations.find(@doctors.find(appointment.doctor_id).organization_id).sliding_scale%></td>
            <td> <%= @doctors.find(appointment.doctor_id).name %> </td>
            <td> <%= @doctors.find(appointment.doctor_id).gender %> </td>
            <td> <%= @doctors.find(appointment.doctor_id).specialty %></td>
            <td> <%= appointment.date %> </td>
            <td><%= link_to 'Book', edit_appointment_path(appointment), {:style =>'color:#FFFFFF;', :class=> "btn btn-lg btn-primary btn-block"} %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
</div>

<div style="margin:none" class="navbar navbar-default navbar-fixed-bottom">
  <div class="container">
    <p class="navbar-text pull-left">© 2017 - D@M:AS.</p>
  </div>
</div>